# 0013: Source registry gates live access

Status: accepted for the offline foundation.

Decision: source registration, fixture eligibility, and future live eligibility are separate versioned fields. The schema permits no live-ready source. A source can be selected only when its fixture retrieval and terms status are explicitly compatible.

Consequence: public availability cannot silently become authorization; future live work requires a deliberate registry change and evaluation update.
