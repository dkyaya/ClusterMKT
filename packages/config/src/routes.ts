export const ROUTES = {
  today: "/",
  cluster: "/clusters/:clusterId",
  watchlist: "/watchlist",
  sectors: "/sectors",
  saved: "/saved",
  listen: "/listen",
  calendar: "/calendar",
  settings: "/settings",
  profile: "/profile",
} as const;
