# `@cluster-mkt/ui`

Accessible, brand-token-driven primitives shared across Cluster MKT interfaces. Semantic components, keyboard behavior, restrained styling, and reduced-motion support belong here. Application routes, demonstration fixtures, evidence decisions, and business logic do not.

The initial package provides Button, Badge, Surface, Tabs, and VisuallyHidden components and imports the canonical brand tokens from `brand/`.
